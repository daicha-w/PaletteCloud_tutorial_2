- if @feed_items.any?
  ol.microposts 
    = render @feed_items
  = will_paginate @feed_items, previous_label: t('shared.feed.will_pagenate.previous_label'), next_label: t('shared.feed.will_pagenate.next_label')